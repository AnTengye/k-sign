services:
  web:
    build: .
    image: k-sign-qinglong:2.20.2
    restart: unless-stopped
    volumes:
      - ./data:/ql/data
    ports:
      - "${QL_BIND_ADDRESS:-0.0.0.0}:${QL_PORT:-5700}:5700"
